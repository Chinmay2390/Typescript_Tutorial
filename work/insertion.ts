function insertion(arr:number[]):number[]{
    let len  = arr.length;
    for(let i = 1;i<len;i++){
        
    }

    return arr;
}


let insert: number[] = [5,4,3,2,1];

console.log(insertion(insert));